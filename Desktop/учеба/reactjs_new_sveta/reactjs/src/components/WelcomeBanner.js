import React, {useState, useEffect} from "react";
import styles from "./../pages/applicants/Rules.module.css";
import backgroundImage from "./../img/pgu-shevchenko.jpg";

const WelcomeBanner = () => {
  /*  const [visible, setVisible] = useState(false);

    useEffect(() => {
        setVisible(true);
    }, []);

return (
    <div className={styles["banner"]} style={{backgroundImage:`url(${backgroundImage})`}}>
        <h1 className={`${styles.text} ${visible ? styles.visible : ''}`}>
            Добро пожаловать на<br/> Физико-математический факультет
        </h1>
    </div>
)*/
}

export default WelcomeBanner;